---
import { ViewTransitions } from "astro:transitions";
import Header from "../components/Header.astro";
import Footer from "./Footer.astro";
import "../styles/css/index.css";
let { url, title, description, image } = Astro.props;
url || (url = import.meta.env.FRONTEND_URL);
title || (title = "Js Tecnologies, la mejor librería para JavaScript");
description || (description = "");
image || image === "https://www.svgrepo.com/show/349419/javascript.svg";
---

<html lang="es">
  <head>
    <meta charset="utf-8" />    
    <meta name="viewport" content="width=device-width" />    
    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={url} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={image} />    
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={url} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={image} />
	<link rel="canonical" href={url}>
	<meta name="generator" content={Astro.generator} />   
	<link rel="shotcut icon" type="image/svg+xml" href="/favicon.svg" />
  <!--Usar ViewTransitions simula una SPA -->
  <ViewTransitions/>
  </head>
  <body >
    <Header />
    <main>
    	<slot />
    </main>
    <Footer />
  </body>
</html>
